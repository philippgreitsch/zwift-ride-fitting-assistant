---
phase: 02-project-scaffold-input-ui
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/App.tsx
  - src/components/wizard/WizardShell.tsx
  - src/components/wizard/StepIndicator.tsx
  - src/components/wizard/SkillLevelSelector.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "User sees a skill level selection screen on first load"
    - "After selecting skill level, user sees a 4-step wizard with tappable step indicators"
    - "User can navigate forward and backward using Next/Back buttons"
    - "User can jump to any step by tapping the step indicator"
    - "Skill level selection is stored and accessible by all wizard steps"
  artifacts:
    - path: "src/components/wizard/WizardShell.tsx"
      provides: "Wizard container with step routing and navigation"
      exports: ["WizardShell"]
    - path: "src/components/wizard/StepIndicator.tsx"
      provides: "Horizontal tappable step row"
      exports: ["StepIndicator"]
    - path: "src/components/wizard/SkillLevelSelector.tsx"
      provides: "Pre-wizard skill level screen"
      exports: ["SkillLevelSelector"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/wizard/WizardShell.tsx"
      via: "renders WizardShell as main content"
      pattern: "<WizardShell"
    - from: "src/components/wizard/WizardShell.tsx"
      to: "src/store/fitStore.ts"
      via: "reads currentStep and skillLevel from store"
      pattern: "useFitStore"
    - from: "src/components/wizard/SkillLevelSelector.tsx"
      to: "src/store/fitStore.ts"
      via: "calls setSkillLevel on selection"
      pattern: "setSkillLevel"
---

<objective>
Build the wizard shell (step routing, navigation, progress indicator) and skill level selector. After this plan, the app renders the full navigation structure with placeholder content for each step.

Purpose: Provide the navigational skeleton that step content plans (03 and 04) populate independently.
Output: Working wizard with skill selector, step indicator, and Next/Back navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-scaffold-input-ui/2-CONTEXT.md
@.planning/phases/02-project-scaffold-input-ui/2-RESEARCH.md
@.planning/phases/02-project-scaffold-input-ui/02-UI-SPEC.md
@.planning/phases/02-project-scaffold-input-ui/02-01-SUMMARY.md

<interfaces>
From src/store/fitStore.ts (created in Plan 01):
```typescript
type SkillLevel = 'beginner' | 'pro'

interface FitStore {
  inputs: FitInputs
  skillLevel: SkillLevel | null
  currentStep: number  // 0 = skill selector, 1-4 = wizard steps, 5 = results
  setSkillLevel: (level: SkillLevel) => void
  setCurrentStep: (step: number) => void
  // ...update methods
}

export const useFitStore: UseBoundStore<StoreApi<FitStore>>
```

shadcn/ui components available:
- Button (variant: "default" | "ghost" | "outline")
- Card, CardContent, CardHeader, CardTitle
- Badge (variant: "default" | "secondary" | "outline")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkillLevelSelector and StepIndicator components</name>
  <files>src/components/wizard/SkillLevelSelector.tsx, src/components/wizard/StepIndicator.tsx</files>
  <read_first>
    - src/store/fitStore.ts (store shape, setSkillLevel, setCurrentStep)
    - src/components/ui/card.tsx (Card component API)
    - src/components/ui/badge.tsx (Badge component API)
    - .planning/phases/02-project-scaffold-input-ui/02-UI-SPEC.md (interaction contracts for skill selector and step indicator)
    - .planning/phases/02-project-scaffold-input-ui/2-CONTEXT.md (D-01, D-06, D-07, D-08, D-09, D-10)
  </read_first>
  <action>
1. Create `src/components/wizard/SkillLevelSelector.tsx`:
   - Renders when `currentStep === 0` (pre-wizard screen per D-07).
   - Heading (28px semibold, UI-SPEC Display role): "How do you want to use this tool?"
   - Two full-width stacked Card components (not a Select dropdown — per UI-SPEC "large tappable option cards"):
     - Option 1: label "I'm new to bike fitting", sub-label (14px zinc-500): "I'll see measurement tips as I go"
     - Option 2: label "I know my measurements", sub-label: "I'll keep tips collapsed"
   - Selected card: `border-2 border-orange-500 bg-orange-500/10`. Unselected: `border border-zinc-200 dark:border-zinc-800 bg-white dark:bg-zinc-950`.
   - Each card has `onClick` that calls `setSkillLevel('beginner')` or `setSkillLevel('pro')` AND `setCurrentStep(1)`.
   - Cards have `min-h-[72px]` for 44px+ touch targets (UX-03).
   - Cards are `cursor-pointer` with `hover:border-zinc-400` on unselected state.

2. Create `src/components/wizard/StepIndicator.tsx`:
   - Props: `currentStep: number` (1-5), `onStepClick: (step: number) => void`.
   - Renders a horizontal row of 5 step items: Physical (1), Body (2), Frame (3), Fit Report (4), Results (5).
   - Each step item: a numbered circle (32px diameter) + label text.
   - Step labels from UI-SPEC Copywriting Contract: "Physical", "Body", "Frame", "Fit Report", "Results".
   - Active step (step === currentStep): circle `bg-orange-500 text-white`, label `font-semibold text-zinc-950 dark:text-zinc-50`.
   - Completed step (step < currentStep): circle `bg-zinc-500 text-white` with lucide `Check` icon (16px) instead of number, label `text-zinc-500`.
   - Upcoming step (step > currentStep): circle `bg-zinc-200 dark:bg-zinc-800 text-zinc-400`, label `text-zinc-400`.
   - All steps are always tappable (D-01): `onClick={() => onStepClick(step)}`, `cursor-pointer`.
   - On screens < 400px (per UI-SPEC Layout Contract): hide labels, show numbers only. Use `hidden sm:inline` on label spans.
   - Row is horizontally scrollable on mobile: `overflow-x-auto flex gap-2` with `min-w-0 flex-shrink-0` on each item.
   - Min height 44px for touch targets (UX-03).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/SkillLevelSelector.tsx exports `SkillLevelSelector`
    - src/components/wizard/SkillLevelSelector.tsx contains `"How do you want to use this tool?"`
    - src/components/wizard/SkillLevelSelector.tsx contains `"I'm new to bike fitting"`
    - src/components/wizard/SkillLevelSelector.tsx contains `"I know my measurements"`
    - src/components/wizard/SkillLevelSelector.tsx contains `setSkillLevel` call
    - src/components/wizard/SkillLevelSelector.tsx contains `setCurrentStep(1)` or equivalent
    - src/components/wizard/SkillLevelSelector.tsx contains `border-orange-500` for selected state
    - src/components/wizard/StepIndicator.tsx exports `StepIndicator`
    - src/components/wizard/StepIndicator.tsx contains labels "Physical", "Body", "Frame", "Fit Report", "Results"
    - src/components/wizard/StepIndicator.tsx contains `bg-orange-500` for active step
    - src/components/wizard/StepIndicator.tsx contains `cursor-pointer` (all steps tappable)
    - src/components/wizard/StepIndicator.tsx contains `Check` import from lucide-react
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>SkillLevelSelector renders two tappable option cards that set skill level and advance to step 1. StepIndicator renders 5 tappable step circles with active/completed/upcoming visual states matching UI-SPEC colors.</done>
</task>

<task type="auto">
  <name>Task 2: Create WizardShell and wire into App.tsx</name>
  <files>src/components/wizard/WizardShell.tsx, src/App.tsx</files>
  <read_first>
    - src/store/fitStore.ts (currentStep, skillLevel, setCurrentStep)
    - src/components/wizard/SkillLevelSelector.tsx (from Task 1)
    - src/components/wizard/StepIndicator.tsx (from Task 1)
    - src/components/ui/button.tsx (Button component API)
    - src/components/ui/card.tsx (Card component API)
    - src/App.tsx (current bare shell)
    - .planning/phases/02-project-scaffold-input-ui/02-UI-SPEC.md (layout contract, copywriting)
    - .planning/phases/02-project-scaffold-input-ui/2-CONTEXT.md (D-01, D-06, D-17, D-18, D-20)
  </read_first>
  <action>
1. Create `src/components/wizard/WizardShell.tsx`:
   - Reads `currentStep`, `skillLevel`, `setCurrentStep` from `useFitStore`.
   - Layout per UI-SPEC:
     - Mobile (< 640px): full viewport width, 16px horizontal padding, no Card chrome — full-bleed content with 24px vertical padding.
     - Desktop (>= 640px): max-w-[600px] centered, wrapped in shadcn `Card` with default border-radius and `p-6` (24px) padding.
   - Top: Display heading (28px semibold): "Zwift Ride Fitting Assistant" — always visible.
   - Below heading: 48px gap (2xl spacing per UI-SPEC) then content area.
   - When `currentStep === 0`: render `<SkillLevelSelector />`.
   - When `currentStep >= 1`: render `<StepIndicator currentStep={currentStep} onStepClick={setCurrentStep} />`.
   - Below step indicator: render step content area. For now, render placeholder divs for steps 1-5:
     - Steps 1-4: `<div className="py-6"><p className="text-zinc-500">Step {n} content — coming soon</p></div>`
     - Step 5 (Results placeholder per D-20): render a Card with heading "Your settings are ready" (28px semibold), body text "We've calculated your Zwift Ride target positions. Full adjustment guide coming in the next update." (16px), sub-line "Come back soon — or check the project for updates." (14px zinc-500).
   - Below step content: navigation buttons.
     - If `currentStep > 1`: show "Back" button (ghost variant): `← Back`, calls `setCurrentStep(currentStep - 1)`.
     - If `currentStep < 5`: show "Next" button (default variant, `bg-orange-500 hover:bg-orange-600 text-white`): `Next →`, calls `setCurrentStep(currentStep + 1)`.
     - If `currentStep === 4`: show CTA instead of Next: "Set up my Zwift Ride →" (same orange accent styling), calls `setCurrentStep(5)`.
     - Mobile layout: buttons stacked vertically (flex-col), Next/CTA first, Back second. Each full-width, min-h-[44px].
     - Desktop layout: buttons inline (flex-row), Back left (ghost), Next/CTA right. min-h-[44px].
   - Each step section should show the appropriate Badge: step 1 gets `<Badge>Start here</Badge>` in orange-500, steps 2-4 get `<Badge variant="secondary">Optional</Badge>`.
   - Section sub-description below badge (per D-05): "Fill in what you have. Anything left blank will be estimated or skipped." (16px zinc-500).

2. Update `src/App.tsx`:
   ```typescript
   import { WizardShell } from './components/wizard/WizardShell'

   function App() {
     return (
       <div className="min-h-screen bg-white dark:bg-zinc-950 font-sans">
         <WizardShell />
       </div>
     )
   }

   export default App
   ```
   - Add dark mode detection: in `src/main.tsx` or `App.tsx`, check `window.matchMedia('(prefers-color-scheme: dark)')` and toggle `class="dark"` on `<html>`. Use a `useEffect` that runs once on mount.

3. Ensure the Results placeholder step (step 5) does NOT show Next/Back — only a "← Back" to return to step 4.
  </action>
  <verify>
    <automated>npm run build && npm run dev -- --host 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/WizardShell.tsx exports `WizardShell`
    - src/components/wizard/WizardShell.tsx contains `useFitStore` import
    - src/components/wizard/WizardShell.tsx contains `<SkillLevelSelector` render
    - src/components/wizard/WizardShell.tsx contains `<StepIndicator` render
    - src/components/wizard/WizardShell.tsx contains `"Zwift Ride Fitting Assistant"` heading
    - src/components/wizard/WizardShell.tsx contains `"Next →"` or `Next →` button text
    - src/components/wizard/WizardShell.tsx contains `"← Back"` or `← Back` button text
    - src/components/wizard/WizardShell.tsx contains `"Set up my Zwift Ride →"` or equivalent CTA text
    - src/components/wizard/WizardShell.tsx contains `"Your settings are ready"` results placeholder heading
    - src/components/wizard/WizardShell.tsx contains `"Start here"` badge text
    - src/components/wizard/WizardShell.tsx contains `"Optional"` badge text
    - src/components/wizard/WizardShell.tsx contains `"Fill in what you have. Anything left blank will be estimated or skipped."` sub-description
    - src/components/wizard/WizardShell.tsx contains `max-w-[600px]` or equivalent max-width
    - src/App.tsx contains `<WizardShell` import and render
    - src/App.tsx contains `dark:bg-zinc-950` for dark mode background
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>App renders full wizard navigation: skill selector on first load, then tappable step indicator with Next/Back buttons, step 5 results placeholder, all steps navigable. Mobile-first layout with 600px max-width on desktop.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npm run dev` shows the app with skill level selector on load
- Clicking either skill option advances to step 1 with step indicator visible
- Next/Back buttons navigate between steps 1-5
- Clicking step indicator circles jumps to any step
- Step 5 shows results placeholder text
- Layout is single-column, centered on desktop, full-bleed on mobile
</verification>

<success_criteria>
- Wizard shell renders correctly with all navigation working (per D-01, D-06)
- Skill level selector shows two tappable cards (per D-07)
- Step indicator shows 5 steps with correct active/completed/upcoming styling
- All steps are tappable for non-linear navigation (per D-01)
- Results step shows placeholder (per D-20)
- "Set up my Zwift Ride" CTA appears on step 4 (per D-18)
- Layout responsive: full-bleed mobile, 600px centered desktop (per UX-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-scaffold-input-ui/02-02-SUMMARY.md`
</output>
