---
phase: 03-output-persistence-and-deploy
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/fitStore.ts
autonomous: true
requirements: [UX-02]
gap_closure: true

must_haves:
  truths:
    - "After progressing past the skill selector and refreshing the page, the user is returned to the step they were on — not step 0"
    - "currentStep is serialized to localStorage under the 'zwift-fit-profile' key alongside inputs and skillLevel"
    - "resetStore still resets currentStep to 0 (via initialState)"
  artifacts:
    - path: "src/store/fitStore.ts"
      provides: "fitStore with currentStep included in partialize"
      contains: "currentStep: state.currentStep"
  key_links:
    - from: "src/store/fitStore.ts partialize"
      to: "localStorage 'zwift-fit-profile'"
      via: "Zustand persist middleware serialization"
      pattern: "currentStep.*state\\.currentStep"
---

<objective>
Include `currentStep` in the Zustand persist `partialize` config so that users return to their last active wizard step after a page refresh.

Purpose: The original decision (logged in STATE.md) was to exclude currentStep — but UAT testing confirmed this was wrong: users are returned to the skill selector (step 0) after every refresh, losing their place mid-wizard. This single-line fix corrects that.

Output: Modified fitStore.ts where partialize includes currentStep: state.currentStep alongside inputs and skillLevel.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-output-persistence-and-deploy/03-HUMAN-UAT.md

<interfaces>
<!-- Current partialize (lines 48-52 of fitStore.ts):         -->
<!--   partialize: (state) => ({                               -->
<!--     inputs: state.inputs,                                 -->
<!--     skillLevel: state.skillLevel,                         -->
<!--     // currentStep intentionally excluded — always ...    -->
<!--   }),                                                      -->

<!-- Required state after this plan:                           -->
<!--   partialize: (state) => ({                               -->
<!--     inputs: state.inputs,                                 -->
<!--     skillLevel: state.skillLevel,                         -->
<!--     currentStep: state.currentStep,                       -->
<!--   }),                                                      -->

<!-- resetStore (lines 40-43) uses set(initialState) which     -->
<!-- sets currentStep: 0 — this continues to work correctly   -->
<!-- because initialState.currentStep = 0.                    -->

<!-- No type changes needed. currentStep is already typed as  -->
<!-- `number` in the FitStore interface and in initialState.  -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include currentStep in fitStore partialize</name>
  <files>src/store/fitStore.ts</files>
  <read_first>
    - src/store/fitStore.ts (MANDATORY — read the full file before editing; confirm the exact lines of the partialize block)
  </read_first>
  <action>
In src/store/fitStore.ts, find the `partialize` callback (currently lines 48-52). Make two changes:

1. Add `currentStep: state.currentStep,` as a new line after `skillLevel: state.skillLevel,`

2. Remove the comment `// currentStep intentionally excluded — always start at step 0 on reload`

The resulting partialize block must be:
```typescript
partialize: (state) => ({
  inputs: state.inputs,
  skillLevel: state.skillLevel,
  currentStep: state.currentStep,
}),
```

No other changes to fitStore.ts. The FitStore interface, initialState, resetStore, and all action functions remain unchanged.
  </action>
  <verify>
    <automated>grep -n "currentStep" /Users/philippgreitsch/Documents/zwift-ride-fitting-assistant/src/store/fitStore.ts</automated>
  </verify>
  <done>
    - `grep -n "currentStep" src/store/fitStore.ts` shows `currentStep: state.currentStep` inside the partialize block
    - The excluded-comment line is gone
    - `npm run build` exits 0
    - `npm test` passes all existing tests
    - STATE.md decision "[Phase 03]: currentStep excluded from persist partialize" is now superseded — note in SUMMARY that this decision was reversed by UAT finding
  </done>
</task>

</tasks>

<verification>
After the task:
1. `grep "currentStep: state.currentStep" src/store/fitStore.ts` returns one match inside the partialize block
2. `grep "intentionally excluded" src/store/fitStore.ts` returns zero matches
3. `npm run build` exits 0
4. `npm test` passes
</verification>

<success_criteria>
- fitStore.ts partialize serializes currentStep to localStorage
- resetStore continues to reset currentStep to 0 (unchanged — it uses set(initialState) and initialState.currentStep = 0)
- UAT Gap 2 is closed: after refresh, user is returned to their last active step, not the skill selector
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-persistence-and-deploy/03-06-SUMMARY.md`
</output>
