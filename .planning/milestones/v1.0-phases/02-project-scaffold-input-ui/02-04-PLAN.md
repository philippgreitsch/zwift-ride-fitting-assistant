---
phase: 02-project-scaffold-input-ui
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/components/wizard/steps/FrameStep.tsx
  - src/components/wizard/steps/FitReportStep.tsx
  - src/components/wizard/steps/ResultsStep.tsx
  - src/components/wizard/WizardShell.tsx
autonomous: true
requirements:
  - FRAME-01
  - FRAME-02
  - FRAME-03
  - FIT-01
  - FIT-02

must_haves:
  truths:
    - "User can enter frame stack, frame reach, and seat tube angle"
    - "Seat tube angle field shows degrees unit, not mm"
    - "User can enter fit report saddle height, saddle fore/aft, handlebar height, and handlebar reach"
    - "Fit Report step shows persistent priority banner: Values here override your physical measurements"
    - "Results step shows placeholder card with Your settings are ready heading"
  artifacts:
    - path: "src/components/wizard/steps/FrameStep.tsx"
      provides: "Frame geometry form with FRAME fields"
      exports: ["FrameStep"]
    - path: "src/components/wizard/steps/FitReportStep.tsx"
      provides: "Fit report form with FIT fields and priority banner"
      exports: ["FitReportStep"]
    - path: "src/components/wizard/steps/ResultsStep.tsx"
      provides: "Phase 2 results placeholder"
      exports: ["ResultsStep"]
  key_links:
    - from: "src/components/wizard/steps/FitReportStep.tsx"
      to: "src/store/fitStore.ts"
      via: "useFitStore for updateFitReport"
      pattern: "updateFitReport"
    - from: "src/components/wizard/steps/FrameStep.tsx"
      to: "src/store/fitStore.ts"
      via: "useFitStore for updateFrame"
      pattern: "updateFrame"
    - from: "src/components/wizard/WizardShell.tsx"
      to: "src/components/wizard/steps/FrameStep.tsx"
      via: "renders FrameStep when currentStep === 3"
      pattern: "<FrameStep"
---

<objective>
Build the Frame Geometry step (3 fields — stack, reach, seat tube angle with degrees unit), Fit Report step (4 fields with priority banner), and Results placeholder step. Wire all three into WizardShell.

Purpose: Complete all remaining wizard step content. After this plan, every step has real form content (or the results placeholder).
Output: Working FrameStep, FitReportStep, and ResultsStep components rendered in the wizard.

NOTE: This plan depends on Plan 02-03 completing first because both plans modify WizardShell.tsx. Plan 02-03 wires steps 1-2; this plan wires steps 3-5. Read WizardShell.tsx fresh at execution time to pick up Plan 02-03's changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-scaffold-input-ui/2-CONTEXT.md
@.planning/phases/02-project-scaffold-input-ui/2-RESEARCH.md
@.planning/phases/02-project-scaffold-input-ui/02-UI-SPEC.md
@.planning/phases/02-project-scaffold-input-ui/02-01-SUMMARY.md
@.planning/phases/02-project-scaffold-input-ui/02-02-SUMMARY.md
@.planning/phases/02-project-scaffold-input-ui/02-03-SUMMARY.md

<interfaces>
From src/store/fitStore.ts:
```typescript
export const useFitStore = create<FitStore>()(...)
// Methods: updateFrame(patch), updateFitReport(patch)
// State: inputs.frame, inputs.fitReport, skillLevel
```

From src/components/wizard/fields/MeasurementField.tsx:
```typescript
interface MeasurementFieldProps {
  id: string; label: string; unit?: 'mm' | 'degrees';
  guidanceLevel: 'simple' | 'complex'; guidanceText: React.ReactNode;
  isOverridden?: boolean; defaultExpanded?: boolean;
  skillLevel: 'beginner' | 'pro' | null; placeholder?: string;
  helperText?: string; error?: string; value?: string;
  onChange?: (value: string) => void; onBlur?: () => void; disabled?: boolean;
}
```

From src/lib/validators.ts:
```typescript
export const frameGeometrySchema = z.object({
  stack: z.number().min(400).max(700).optional(),
  reach: z.number().min(350).max(450).optional(),
  seatTubeAngle: z.number().min(70).max(80).optional(),
})

export const fitReportSchema = z.object({
  saddleHeight: z.number().positive().optional(),
  saddleForeAft: z.number().positive().optional(),
  handlebarHeight: z.number().positive().optional(),
  handlebarReach: z.number().positive().optional(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FrameStep, FitReportStep, and ResultsStep</name>
  <files>src/components/wizard/steps/FrameStep.tsx, src/components/wizard/steps/FitReportStep.tsx, src/components/wizard/steps/ResultsStep.tsx</files>
  <read_first>
    - src/store/fitStore.ts (updateFrame, updateFitReport, skillLevel)
    - src/components/wizard/fields/MeasurementField.tsx (props interface)
    - src/lib/validators.ts (frameGeometrySchema, fitReportSchema — field ranges)
    - src/components/ui/card.tsx (Card component for ResultsStep)
    - .planning/phases/02-project-scaffold-input-ui/02-UI-SPEC.md (field inventory, interaction contracts for fit report banner, results placeholder, copywriting)
    - .planning/phases/02-project-scaffold-input-ui/2-CONTEXT.md (D-04, D-05, D-15, D-16, D-17, D-20)
  </read_first>
  <action>
1. Create `src/components/wizard/steps/FrameStep.tsx`:
   - Same RHF + Zod pattern as PhysicalStep/BodyStep: `useForm` with `mode: 'onBlur'`, preprocessed optional numbers.
   - Zod schema ranges from frameGeometrySchema:
     - stack: min 400, max 700
     - reach: min 350, max 450
     - seatTubeAngle: min 70, max 80 (DEGREES — per Pitfall 8)
   - Three MeasurementField renders:
     - Frame stack: label "Frame stack", unit `mm`, guidanceLevel "simple", guidance: "Found on your bike manufacturer's geometry chart. Measured vertically from the center of the bottom bracket to the top of the head tube."
     - Frame reach: label "Frame reach", unit `mm`, guidanceLevel "simple", guidance: "Found on your bike manufacturer's geometry chart. Measured horizontally from the center of the bottom bracket to the top-center of the head tube."
     - Seat tube angle: label "Seat tube angle", unit `degrees` (NOT mm — critical per Pitfall 8), guidanceLevel "simple", guidance: "Found on your bike manufacturer's geometry chart. Typical road bikes are 72-75 degrees."
   - For seat tube angle validation error: "Enter a value between 70 and 80 degrees." (not mm).
   - Default values from Zustand: `useFitStore(s => s.inputs.frame)`.
   - On blur: sync to Zustand via `updateFrame`.
   - First field (stack) gets `defaultExpanded={skillLevel === 'beginner'}`.
   - NOTE: FRAME-03 requires only seat tube angle (no seat tube length). The FitInputs.frame type contract has seatTubeAngle but no seatTubeLength field — the calculation engine does not use seat tube length.

2. Create `src/components/wizard/steps/FitReportStep.tsx`:
   - Same RHF + Zod pattern with preprocessed optional numbers.
   - Zod: all four fields are positive optional numbers (no upper range — fit reports can have any reasonable value).
   - **Priority banner at top (D-15):** A non-dismissable info banner with:
     - Background: `bg-amber-50 dark:bg-amber-950/30`
     - Border: `border border-amber-200 dark:border-amber-800`
     - Rounded: `rounded-lg`
     - Padding: `p-4`
     - Icon: lucide `AlertCircle` in `text-amber-600`, 16px, inline before text.
     - Text (14px, `text-zinc-700 dark:text-zinc-300`): "Values here override your physical measurements for the same axis."
   - Four MeasurementField renders:
     - Fit saddle height: label "Saddle height (fit report)", unit mm, guidanceLevel "simple", guidance: "Enter the saddle height value from your bike fit report."
     - Fit saddle fore/aft: label "Saddle fore/aft (fit report)", unit mm, guidanceLevel "simple", guidance: "Enter the saddle setback or fore/aft value from your bike fit report."
     - Fit handlebar height: label "Handlebar height (fit report)", unit mm, guidanceLevel "simple", guidance: "Enter the handlebar height value from your bike fit report."
     - Fit handlebar reach: label "Handlebar reach (fit report)", unit mm, guidanceLevel "simple", guidance: "Enter the handlebar reach value from your bike fit report."
   - Default values from Zustand: `useFitStore(s => s.inputs.fitReport)`.
   - On blur: sync to Zustand via `updateFitReport`.
   - First field gets `defaultExpanded={skillLevel === 'beginner'}`.

3. Create `src/components/wizard/steps/ResultsStep.tsx`:
   - Per D-20 / UI-SPEC Results Placeholder:
   - Renders a shadcn `Card` with:
     - Heading (28px semibold, `text-2xl font-semibold`): "Your settings are ready"
     - Body (16px): "We've calculated your Zwift Ride target positions. Full adjustment guide coming in the next update."
     - Sub-line (14px, `text-zinc-500`): "Come back soon — or check the project for updates."
   - No form fields, no data display — pure placeholder for Phase 3.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/steps/FrameStep.tsx exports `FrameStep`
    - FrameStep.tsx contains `<MeasurementField` renders (3 instances)
    - FrameStep.tsx contains `"Frame stack"` label
    - FrameStep.tsx contains `"Frame reach"` label
    - FrameStep.tsx contains `"Seat tube angle"` label
    - FrameStep.tsx contains `unit="degrees"` or `unit={'degrees'}` for seat tube angle (NOT mm)
    - FrameStep.tsx contains `updateFrame` call
    - FrameStep.tsx does NOT contain any `seatTubeLength` field (FRAME-03 is angle only per FitInputs type contract)
    - src/components/wizard/steps/FitReportStep.tsx exports `FitReportStep`
    - FitReportStep.tsx contains `<MeasurementField` renders (4 instances)
    - FitReportStep.tsx contains `"Saddle height (fit report)"` label
    - FitReportStep.tsx contains `"Values here override your physical measurements for the same axis."` banner text
    - FitReportStep.tsx contains `AlertCircle` import from lucide-react
    - FitReportStep.tsx contains `bg-amber-50` for banner background
    - FitReportStep.tsx contains `updateFitReport` call
    - src/components/wizard/steps/ResultsStep.tsx exports `ResultsStep`
    - ResultsStep.tsx contains `"Your settings are ready"` heading
    - ResultsStep.tsx contains `"Full adjustment guide coming in the next update"` or equivalent body text
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>FrameStep renders 3 fields (stack, reach, seat tube angle) with seat tube angle using degrees unit. No seat tube length field — FRAME-03 covers angle only per FitInputs type contract. FitReportStep renders 4 fields with persistent amber priority banner. ResultsStep shows Phase 2 placeholder. All build successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FrameStep, FitReportStep, and ResultsStep into WizardShell</name>
  <files>src/components/wizard/WizardShell.tsx</files>
  <read_first>
    - src/components/wizard/WizardShell.tsx (current state — MUST read fresh after Plan 02-03 has wired PhysicalStep and BodyStep into steps 1-2)
    - src/components/wizard/steps/FrameStep.tsx (just created)
    - src/components/wizard/steps/FitReportStep.tsx (just created)
    - src/components/wizard/steps/ResultsStep.tsx (just created)
  </read_first>
  <action>
1. In `src/components/wizard/WizardShell.tsx`:
   - Import `FrameStep`, `FitReportStep`, `ResultsStep` from `./steps/`.
   - Replace step 3 placeholder with `<FrameStep />`.
   - Replace step 4 placeholder with `<FitReportStep />`.
   - Replace step 5 placeholder with `<ResultsStep />`.
   - If step 5 was already hardcoded as a results placeholder in Plan 02, extract it into the `ResultsStep` component and render `<ResultsStep />` instead.

2. Verify navigation flow:
   - Step 1: PhysicalStep (wired by Plan 02-03)
   - Step 2: BodyStep (wired by Plan 02-03)
   - Step 3: FrameStep
   - Step 4: FitReportStep (with "Set up my Zwift Ride" CTA instead of "Next")
   - Step 5: ResultsStep (only Back button, no Next)

3. Ensure all step placeholders are fully replaced — no more "Step N content — coming soon" text remains in WizardShell.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/WizardShell.tsx contains `import.*FrameStep`
    - src/components/wizard/WizardShell.tsx contains `import.*FitReportStep`
    - src/components/wizard/WizardShell.tsx contains `import.*ResultsStep`
    - src/components/wizard/WizardShell.tsx contains `<FrameStep` render
    - src/components/wizard/WizardShell.tsx contains `<FitReportStep` render
    - src/components/wizard/WizardShell.tsx contains `<ResultsStep` render
    - src/components/wizard/WizardShell.tsx does NOT contain `coming soon` placeholder text
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All 5 wizard steps are wired into WizardShell. No placeholders remain. Full wizard navigation works from skill selector through all 4 form steps to results placeholder.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Frame step renders 3 fields with seat tube angle in degrees (no seat tube length)
- Fit Report step renders priority banner + 4 fields
- Results step renders placeholder card
- All 5 steps render real content (no more placeholders)
- Navigation: skill selector -> physical -> body -> frame -> fit report -> results
</verification>

<success_criteria>
- FRAME-01 and FRAME-02 addressed by FrameStep stack and reach fields
- FRAME-03 addressed by FrameStep seat tube angle field (angle only — no seat tube length in FitInputs type contract)
- FIT-01 addressed by FitReportStep fields
- FIT-02 addressed by priority banner (D-15) — override state on physical fields already handled in Plan 03
- Results placeholder matches D-20 and UI-SPEC copywriting exactly
- Seat tube angle uses degrees unit, not mm (Pitfall 8 avoided)
- All steps wired into WizardShell with correct navigation
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-scaffold-input-ui/02-04-SUMMARY.md`
</output>
